\appendix
\chapter{Shrinking sphere centering method}
\label{app:shrinking_sphere}

This appendix gives the mathematical formulation and a direct algorithmic implementation of the shrinking-sphere centering routine used in the analysis.

Let the particle set be indexed by $i$, with positions $\mathbf{x}_i\in\mathbb{R}^3$ and masses $m_i>0$. Define the mass-weighted centroid over an index set $S$ as
\begin{equation}
\mathbf{C}(S) = \frac{\sum_{i\in S} m_i\,\mathbf{x}_i}{\sum_{i\in S} m_i}.
\end{equation}

Initialize
\begin{equation}
S_0 = \{1,\dots,N\},\qquad \mathbf{x}_0 = \mathbf{C}(S_0),\qquad R_0 = \max_{i\in S_0}\|\mathbf{x}_i-\mathbf{x}_0\|.
\end{equation}

For iterations $k=0,1,2,\dots$ define the restricted set and update rules
\begin{align}
S_{k+1} &= \{i\in S_k:\ \|\mathbf{x}_i-\mathbf{x}_k\| < R_k\}, \\
\mathbf{x}_{k+1} &= \mathbf{C}(S_{k+1}), \\
R_{k+1} &= f_s\,R_k,
\end{align}
with shrink factor $0<f_s<1$ (implementation default $f_s=0.9$).

Stop when either
\begin{equation}
\|\mathbf{x}_{k+1}-\mathbf{x}_k\| < \delta \quad\text{or}\quad |S_{k+1}| < N_{\min},
\end{equation}
and return $\mathbf{x}_{\mathrm{final}}=\mathbf{x}_{k+1}$. Typical choices used in the analysis are $\delta=10^{-5}$ (code length units) and $N_{\min}=100$.

The same logic can be written in compact pseudocode as follows:
\begin{lstlisting}[basicstyle=\footnotesize\ttfamily,breaklines=true]
function shrinking_sphere_center(pos[N,3], mass[N], f_s=0.9, delta=1e-5, N_min=100):
	x = weighted_average(pos, mass)
	R = max(norm(pos - x, axis=1))
	while True:
		r = norm(pos - x, axis=1)
		mask = (r < R)
		if mask.sum() < N_min:
			break
		x_new = weighted_average(pos[mask], mass[mask])
		if norm(x_new - x) < delta:
			x = x_new
			break
		x = x_new
		R = R * f_s
	return x
\end{lstlisting}


\chapter{Orbits}
\label{app:tnfw_orbits}
This appendix presents additional orbital trajectories for a selection of representative particles in the hybrid simulation with $R_{\rm cut}=10$ kpc.

\begin{figure}[H]
	\centering
	\includegraphics[width=0.7\textwidth]{content/ch5/img/orbit_01.png}
	\caption{Orbit of selected particle 1}
	\label{fig:tnfw_validation_orbits_1}
\end{figure}

\begin{figure}[H]
	\centering
	\includegraphics[width=0.7\textwidth]{content/ch5/img/orbit_02.png}
	\caption{Orbit of selected particle 2}
	\label{fig:tnfw_validation_orbits_2}
\end{figure}

\begin{figure}[H]
	\centering
	\includegraphics[width=0.7\textwidth]{content/ch5/img/orbit_03.png}
	\caption{Orbit of selected particle 3}
	\label{fig:tnfw_validation_orbits_3}
\end{figure}

\begin{figure}[H]
	\centering
	\includegraphics[width=0.7\textwidth]{content/ch5/img/orbit_04.png}
	\caption{Orbit of selected particle 4}
	\label{fig:tnfw_validation_orbits_4}
\end{figure}



\chapter{Truncated NFW density profile evolution}
\label{app:tnfw_density_profiles}
This appendix contains the full density profile plots for the set of small $R_{\rm cut}$ simulations considered in the study.

\begin{figure}[H]
	\centering
	\includegraphics[width=0.8\textwidth]{content/ch5/img/5.png}
	\caption{Density profile evolution for the truncated-NFW test with cutoff at 5 kpc.}
\end{figure}

\begin{figure}[H]
	\centering
	\includegraphics[width=0.8\textwidth]{content/ch5/img/0.5.png}
	\caption{Density profile evolution for the truncated-NFW test with cutoff at 0.5 kpc.}
\end{figure}

\begin{figure}[H]
	\centering
	\includegraphics[width=0.8\textwidth]{content/ch5/img/0.05.png}
	\caption{Density profile evolution for the truncated-NFW test with cutoff at 0.05 kpc.}
\end{figure}

\begin{figure}[H]
	\centering
	\includegraphics[width=0.8\textwidth]{content/ch5/img/0.03.png}
	\caption{Density profile evolution for the truncated-NFW test with cutoff at 0.03 kpc.}
\end{figure}

\begin{figure}[H]
	\centering
	\includegraphics[width=0.8\textwidth]{content/ch5/img/0.02.png}
	\caption{Density profile evolution for the truncated-NFW test with cutoff at 0.02 kpc.}
\end{figure}