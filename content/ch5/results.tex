\chapter{Results and discussion}

This chapter presents the numerical results from the simulation campaign and the accompanying sensitivity studies. The baseline evolution of the reference halo and the principal diagnostics are described first; subsequent sections examine robustness to analysis choices, numerical controls, domain size and initial inner slope. Truncated‑NFW hybrid experiments are used to isolate the influence of long range potential contributions.

\section{Baseline behaviour}
\label{sec:results_baseline}

The baseline run shows a clear cusp-to-core transition, but the core does not stabilize. Instead, the inner density continues to decline throughout the simulated time span. This is the central empirical result of the thesis. In the broader context of halo evolution, the literature shows that collisionless systems can display a wide range of inner responses to external perturbations and time-dependent forcing, including stochastic heating by substructure and repeated diffusion in energy angular momentum space \citep{penarrubia2017fluctuations,penarrubia2019stochastic}. Recent controlled $N$-body models by \cite{errani2026collisional} show that collisional star dark matter energy exchange can heat a cusp into a core and that this evolution tends to self-regulate as central dark matter densities drop and binary formation increases, i.e. the contraction/heating rates slow at late times.

If that late-time stabilization picture applied directly to this setup, the inner density would be expected to flatten after core formation rather than continue declining. The reference run is therefore notable not because it forms a core, but because the core remains dynamically active rather than approaching a steady inner state.

%The result is also instructive from the point of view of numerical convergence. \cite{power2003inner} showed that the inner halo is especially sensitive to the interplay between resolution, force softening, and timestep control, while \cite{zhang2019optimal} later demonstrated that too aggressive a reduction of the softening length can artificially raise the innermost density. The reference simulation does not display a simple monotonic dependence on any one of these parameters, which already suggests that the behaviour is not a trivial one-parameter resolution effect.

To make these statements quantitative a few standard diagnostic definitions are used. All radial profiles are measured in a sequence of spherical shells extending from the halo centre to a maximum radius $r_{\rm max}$. Rather than adopting uniformly spaced bins, a logarithmically compressed coordinate is employed in order to increase the spatial resolution in the central region while maintaining adequate sampling at large radii. The transformation is defined as
\begin{equation}
u(r)=\ln\!\left(1+\frac{r}{r_c}\right),
\end{equation}
where $r_c$ is a characteristic scale radius. The radial bin boundaries are uniformly spaced in the transformed coordinate $u$, and subsequently mapped back to physical radii through
\begin{equation}
r(u)=r_c\left(e^u-1\right).
\end{equation}
This construction gives narrow bins near the halo centre and progressively wider bins at larger radii, reducing shot noise while preserving the ability to resolve the inner density structure.

The spherically averaged density profile is obtained by dividing the halo into radial shells and computing the mass enclosed within each shell. If $\Delta M_k$ denotes the mass contained in the shell bounded by radii $r_k$ and $r_{k+1}$, the density is estimated as
\begin{equation}
\rho(r_k)
=
\frac{\Delta M_k}
{\Delta V_k},
\end{equation}
where the shell volume is
\begin{equation}
\Delta V_k
=
\frac{4\pi}{3}
\left(
r_{k+1}^3-r_k^3
\right).
\end{equation}
The resulting profile $\rho(r)$ provides the primary diagnostic for identifying the formation of a central core or other structural changes within the halo. Profiles are compared to the initial state using ratio curves $\rho(r,t)/\rho(r,t_0)$, which makes the central flattening immediately clear.

%The first two images below provide a static view of the baseline system at different scales. Figure~\ref{fig:baseline_particle_map} shows how the mass is distributed across the inner halo and its surroundings, while Figure~\ref{fig:baseline_potential_map} illustrates how the gravitational well remains concentrated near the center even as the viewing aperture expands. Taken together, these plots complement the time-evolution diagnostics by showing the same baseline behaviour from a spatial perspective.

%\begin{figure}[H]
%	\centering
%	\includegraphics[width=1\textwidth]{content/ch5/img/particle_map_3panel.png}
%	\caption{\textbf{Particle distribution of the simulation.} The same system is shown at three increasing apertures $r_{\mathrm{max}}$.}
%	\label{fig:baseline_particle_map}
%\end{figure}

%\begin{figure}[H]
%	\centering
%	\includegraphics[width=1\textwidth]{content/ch5/img/potential_map_multi_rmax.png}
%	\caption{\textbf{Multiscale view of the gravitational potential.} The same particle distribution is shown with progressively increasing aperture size $r_{\mathrm{max}}$, revealing how the potential structure transitions from a strongly centralised well at small radii to a more extended field at larger scales. The deepest regions remain consistently centred, while outer structure becomes increasingly visible as the field of view expands.}
%	\label{fig:baseline_potential_map}
%\end{figure}

In the plot in Figure~\ref{fig:baseline_gradient} the central density drops rapidly at early times, then continues to drift downward more slowly, without ever forming a stable plateau. In the ratio to the initial conditions, the decline is easier to read at a glance. In that sense, the evolution resembles a continuing redistribution of mass rather than a completed structural transformation. This is the key point that motivates the rest of the chapter.

\begin{figure}[H]
	\centering
	\includegraphics[width=0.9\textwidth]{content/ch5/img/gradient.png}
	\caption{\textbf{Baseline density profile evolution.} The central density decreases steadily over time, with the steepest change occurring early and the later evolution approaching a shallower, but still declining, trend. The vertical dashed line indicates the softening reference radius $r_{\rm ref} = 2.8\,\epsilon$. The lower panel shows the density ratio relative to the initial conditions.}
	\label{fig:baseline_gradient}
\end{figure}

%\begin{figure}[H]
%	\centering
%	\includegraphics[width=0.9\textwidth]{content/ch5/img/density_evolution_cusp.png}
%	\caption{\textbf{Baseline evolution with ratio to the initial state.} The upper panel shows the density evolution over time. The lower panel shows the density ratio relative to the initial conditions.}
%	\label{fig:baseline_cusp_short}
%\end{figure}

\subsection{Snapshot centering}
Centering is handled independently from the density estimation. Each snapshot is recentered using a shrinking sphere algorithm, which iteratively refines the halo center by repeatedly restricting the particle set to smaller radii around the current estimate. If the particle positions are $\mathbf{x}_i$ and masses are $m_i$, the mass-weighted centroid of a selected set $S_k$ is
\begin{equation}
\mathbf{x}_k = \mathbf{C}(S_k) = \frac{\sum_{i\in S_k} m_i\,\mathbf{x}_i}{\sum_{i\in S_k} m_i},
\end{equation}
with the restricted set updated as
\begin{align}
S_{k+1} &= \left\{i\in S_k:\ \|\mathbf{x}_i-\mathbf{x}_k\| < R_k\right\}, \\
R_{k+1} &= f_s R_k,
\end{align}
for a shrink factor $0<f_s<1$.
This method is robust against mild asymmetries and against the gradual drift that can occur in long collisionless runs.

In practice, this removes one of the most obvious numerical artefacts that could otherwise be mistaken for a central density decline. The iteration stops once the centroid shift becomes small, $\|\mathbf{x}_{k+1}-\mathbf{x}_k\|<\delta$, or the retained set becomes too small, $|S_{k+1}|<N_{\min}$. A formal description of the algorithm and the exact analysis implementation are given in Appendix~\ref{app:shrinking_sphere}.

\subsection{Kinematic heating analysis}

In an ideal collisionless dark matter halo, particles evolve within a smooth gravitational potential and should not exchange significant amounts of energy through direct encounters. However, in an $N$-body simulation the finite number of particles introduces discreteness noise, allowing weak particle-particle interactions to accumulate over time \citep{power2003inner, diemand2004relaxation}.

The hallmark of this process is a gradual and approximately linear increase in the velocity dispersion or in the anisotropy parameter, $\beta$, throughout the entire simulation, even over timescales as long as $100\,\mathrm{Gyr}$. This behavior indicates that the system is being artificially heated by numerical effects rather than evolving due to physical mechanisms \citep{diemand2004relaxation}.

The consequences of two-body relaxation can be significant. Continuous energy exchange between particles can alter orbital distributions, modify the velocity structure of the halo, and potentially generate artificial core growth or changes in the density profile \citep{elzant2006relaxation, baushev2013convergence}. Consequently, detecting a long-term monotonic increase in velocity dispersion or anisotropy serves as strong evidence that numerical relaxation is influencing the simulation results.

To quantify whether the apparent core evolution is driven by genuine mass redistribution or by numerical/physical heating, the second moments of the velocity distribution in spherical shells are examined. For each snapshot, the halo is first recentered and the bulk velocity is subtracted from all particles. For a particle $i$ at position $\boldsymbol{x}_i$, its distance from the center is defined as,
\begin{equation}
r_i = |\boldsymbol{x}_i| ,
\end{equation}
and the corresponding unit radial vector,
\begin{equation}
\hat{\boldsymbol{r}}_i = \frac{\boldsymbol{x}_i}{r_i}.
\end{equation}
The particle velocity relative to the bulk motion is denoted by $\boldsymbol{v}_i$.

The radial component of the velocity is then
\begin{equation}
v_{r,i} = \boldsymbol{v}_i \cdot \hat{\boldsymbol{r}}_i ,
\end{equation}
while the tangential velocity is obtained from the decomposition
\begin{equation}
v_{t,i}^2 = |\boldsymbol{v}_i|^2 - v_{r,i}^2 .
\end{equation}

The particles are grouped into spherical radial bins centered at radius $r_k$. In each bin, the velocity dispersions as the unbiased variances of the corresponding velocity components are computed:
\begin{equation}
\sigma_r^2(r_k) = \left\langle v_r^2 \right\rangle_k - \left\langle v_r \right\rangle_k^2 ,
\end{equation}
\begin{equation}
\sigma_t^2(r_k) = \left\langle v_t^2 \right\rangle_k - \left\langle v_t \right\rangle_k^2 ,
\end{equation}
where $\langle \cdot \rangle_k$ denotes an average over all particles in the $k$-th shell. Equivalently, one may compute the total velocity variance in the shell and subtract the radial contribution:
\begin{equation}
\sigma_t^2(r_k) = \sigma_{\rm tot}^2(r_k) - \sigma_r^2(r_k),
\end{equation}
with
\begin{equation}
\sigma_{\rm tot}^2(r_k) = \sigma_x^2(r_k) + \sigma_y^2(r_k) + \sigma_z^2(r_k).
\end{equation}

From these quantities, the velocity anisotropy parameter is defined as
\begin{equation}
\beta(r_k) = 1 - \frac{\sigma_t^2(r_k)}{2\,\sigma_r^2(r_k)}.
\end{equation}
An isotropic velocity distribution corresponds to $\beta = 0$, while $\beta > 0$ indicates radially biased orbits \citep{binney2008galactic}. In the context of core evolution, a sustained increase in $\sigma_r$ and/or a systematic rise in $\beta$ in the innermost shells would signal kinematic heating rather than simple adiabatic expansion \citep{diemand2004relaxation, elzant2006relaxation}.

To further characterize the system's kinematic state, the one-dimensional velocity dispersion is computed. The total one-dimensional velocity dispersion, $\sigma_{\mathrm{1D}}(r)$, is defined via its radial ($\sigma_r$) and tangential ($\sigma_t$) components as:
\begin{equation}
    \sigma_{\mathrm{1D}}(r) = \sqrt{\frac{\sigma_r^2(r) + \sigma_t^2(r)}{3}} = \sqrt{\frac{\sigma_{\mathrm{tot}}^2(r)}{3}}.
    \label{eq:sigma_1d}
\end{equation}

Because the kinematic profiles are measured from finite particle numbers, the raw binned quantities exhibit statistical noise, especially at small radii. To improve visual clarity while preserving trends, the profiles are optionally smoothed with a one-dimensional Gaussian kernel in radius. For a generic profile $X(r_k)$, the smoothed profile is defined as
\begin{equation}
\widetilde{X}(r_k) =
\frac{\displaystyle \sum_j X(r_j)\,
\exp\!\left[-\frac{(r_k-r_j)^2}{2h^2}\right]}
{\displaystyle \sum_j \exp\!\left[-\frac{(r_k-r_j)^2}{2h^2}\right]},
\end{equation}
where $h$ is the smoothing length. In practice, $h$ is chosen to be comparable to one radial bin width, so that the filter suppresses shot noise without erasing genuine radial trends. The same procedure is applied consistently to $\sigma_r(r)$, $\sigma_t(r)$, and $\beta(r)$.

%For a collisionless stellar system in steady-state, spherical hydrostatic equilibrium, the velocity dispersion profiles and the spatial density distribution $\rho(r)$ must satisfy the spherical Jeans equation:
%\begin{equation}
%   \frac{\mathrm{d}}{\mathrm{d}r}\left[\rho(r) \sigma_r^2(r)\right] + \frac{2\beta(r)}{r}\rho(r) \sigma_r^2(r) = -\rho(r)\frac{\mathrm{d}\Phi}{\mathrm{d}r} = -\frac{G M(<r)\rho(r)}{r^2},
%    \label{eq:spherical_jeans}
%\end{equation}
%where $\beta(r)$ is the stellar anisotropy parameter defined previously, $\Phi(r)$ is the global gravitational potential, and $M(<r)$ represents the cumulative halo mass contained within a sphere of radius $r$. 

The above diagnostics are computed at multiple simulation times, and the resulting profiles $\sigma_{\mathrm{1D}}(r)$ and $\beta(r)$ are compared between the initial state and late-time snapshots in Figure~\ref{fig:baseline_kinematics}.

\begin{figure}[H]
	\centering
	\includegraphics[width=0.9\textwidth]{content/ch5/img/baseline_kinematics.png}
	\caption{Velocity dispersion $\sigma_{\mathrm{1D}}(r)$ and orbital anisotropy $\beta(r)$ evolution up to 100 Gyr for a softening length value of $\epsilon = 0.01$ kpc.}
	\label{fig:baseline_kinematics}
\end{figure}

The evolution of the isolated halo provides clear evidence for numerical core heating over the full $100\ \mathrm{Gyr}$ integration. In the inner region, $r \lesssim 0.1\ \mathrm{kpc}$, the one-dimensional velocity dispersion, $\sigma_{\rm 1D}$, increases continuously from its initial value of approximately $6\ \mathrm{km\,s^{-1}}$ to nearly $9\ \mathrm{km\,s^{-1}}$ by the end of the run. This trend is physically significant because an expanding, declining density system would be expected to cool adiabatically; instead, the halo becomes kinematically hotter as the central density decreases. The simultaneous drop in density and rise in $\sigma_{\rm 1D}$ therefore rules out adiabatic expansion and strongly indicates non-adiabatic heating. For an isolated system, this behavior is most naturally interpreted as numerical heating driven by discreteness noise and two-body relaxation. In this interpretation, spurious energy injection gradually puffs up the central region, lowering the density while increasing the random particle motions.

In contrast to the steady chronological rise observed in the velocity dispersion, the orbital anisotropy parameter, $\beta$, does not exhibit a directional evolution over time. The initial condition begins at a non-zero value, and subsequent snapshots appear heavily intermingled, fluctuating without a clear chronological sequence in the inner regions ($r \lesssim 0.1\ \mathrm{kpc}$). This lack of a coherent temporal trend indicates that while numerical heating continuously injects kinetic energy into the halo core, it does not alter the underlying orbital geometry of the system over $100\ \mathrm{Gyr}$. Instead, chaotic nature of the $\beta$ profiles highlights that the inner velocity architecture is dominated by snapshot-to-snapshot stochastic noise. This high variability is likely driven by low particle numbers in the innermost log-spaced bins, or by minor physical sloshing of the halo core relative to the calculated center of mass, which dynamically redistributes the measured radial and tangential velocity components from one snapshot to the next.

\section{Box size}
\label{sec:results_box_size}

The role of the numerical domain was tested explicitly by repeating the reference experiment in three increasingly larger simulation volumes while holding all physical and numerical parameters fixed (identical particle realization, softening, timestepping rules and gravity solver settings). The objective was to determine whether finite domain effects or boundary condition artefacts can explain the persistent central density decline.

Figure~\ref{fig:box_size_study} summarises the outcome. The central density diagnostic used throughout this thesis shows the same secular decline in each volume. Enlarging the domain alters the quantitative rate of depletion by a modest amount, but it does not halt or reverse the process; the qualitative behaviour is invariant to the choice of box size.

There are two simple physical interpretations of this result. First, long range mass beyond the immediate inner region changes the background potential and can therefore modify rates \citep{binney2008galactic}. Second, if the mechanism were purely an artefact of a truncated domain (for example a spurious boundary tide or reflection), one would expect the effect to vanish as the box is enlarged; the absence of such a disappearance argues against this idea. These conclusions are in line with standard convergence diagnostics for collisionless codes \citep{power2003inner} and with the expectation that stochastic long range perturbations modulate rates rather than create new inner dynamics \citep{penarrubia2017fluctuations,penarrubia2019stochastic}.

\begin{figure}[H]
	\centering
	\includegraphics[width=0.9\textwidth]{content/ch5/img/box_size_study.png}
	\caption{\textbf{Box size study.} Comparison of the three tested simulation volumes (100, 200, 500 $kpc^3$). The central density diagnostic shows the same secular decline across volumes.}
	\label{fig:box_size_study}
\end{figure}

\section{Initial halo structure}
\label{sec:results_initial_structure}

The comparison between cuspy and cored initial conditions is particularly informative because it distinguishes between a cusp specific instability and a more general process. The core run still exhibits a decline of the central density, but the amplitude is smaller than in the cuspy case. Shown in Figure~\ref{fig:core_gradient}, this is a strong result because it means the mechanism is not limited to the special case of an initially steep central profile.

%\begin{figure}[H]
%	\centering
%	\includegraphics[width=1\textwidth]{content/ch5/img/particle_map_3panel_core.png}
%	\caption{\textbf{Particle distribution of the core simulation.} The same system is shown at three increasing apertures $r_{\mathrm{max}}$.}
%	\label{fig:core_particle_map}
%\end{figure}

% The same pair of spatial diagnostics is repeated for the cored run so that the two initial condition setups can be compared directly. Figure~\ref{fig:core_particle_map} highlights the different central concentration of the cored halo, and Figure~\ref{fig:core_potential_map} shows how that difference carries over into a shallower inner well. These figures make it easier to relate the profile evolution to the underlying spatial structure of the system.

%\begin{figure}[H]
%	\centering
%	\includegraphics[width=1\textwidth]{content/ch5/img/potential_map_multi_rmax_core.png}
%	\caption{\textbf{Multiscale view of the core gravitational potential.} The same particle distribution is shown with progressively increasing aperture size $r_{\mathrm{max}}$, revealing how the potential structure transitions from a strongly centralised well at small radii to a more extended field at larger scales. The deepest regions remain consistently centred, while outer structure becomes increasingly visible as the field of view expands.}
%	\label{fig:core_potential_map}
%\end{figure}

In physical terms, this suggests that the system is sensitive to the presence of a central potential well, but not exclusively to its initial logarithmic slope. A cored halo has less extreme inner structure and therefore reacts less strongly, yet the evolution remains qualitatively the same. This is particularly important in light of the \cite{errani2026collisional} result that cusp evolution should weaken once the core is established and central dark matter densities are reduced. In that scenario, a pre-existing core should not continue to lose central density over long timescales; however, this behaviour is observed in the simulations. That mismatch reinforces the conclusion that the persistent decline cannot be explained as a simple cusp-to-core transient and is instead tied to a broader dynamical mechanism.

The literature on heating caused by substructure is again relevant here. Stochastic forcing does not require a cusp; it requires a fluctuating potential and a population of perturbers that can exchange energy with the central region \citep{penarrubia2017fluctuations}. The persistence of the effect in the core run is therefore compatible with a heating picture and inconsistent with the idea that the problem is restricted to cuspy initial conditions.

\begin{figure}[H]
	\centering
	\includegraphics[width=0.9\textwidth]{content/ch5/img/gradient_core.png}
	\caption{\textbf{Core initial condition density profile evolution.} The upper panel shows the density evolution over time. The lower panel shows the density ratio relative to the initial conditions.}
	\label{fig:core_gradient}
\end{figure}

%\begin{figure}[H]
%	\centering
%	\includegraphics[width=0.9\textwidth]{content/ch5/img/density_evolution_core.png}
%	\caption{\textbf{Core initial condition evolution with ratio to the initial state.} The upper panel shows the density evolution over time. The lower panel shows the density ratio relative to the initial conditions.}
%	\label{fig:core_initial_condition}
%\end{figure}

\section{Gravitational softening}
\label{sec:results_softening}

Softening is the most obvious numerical parameter to suspect in any study of the inner halo, because it directly regularizes short range interactions. The literature makes clear, however, that the role of softening is subtle: too much softening suppresses small scale structure, while too little can artificially increase the density in the innermost region \citep{power2003inner,zhang2019optimal}. In the convergence analysis of \cite{zhang2019optimal}, the optimal softening length is the one that balances force bias against discreteness noise; below that scale, the central profile can become biased high because close encounters are less regularized and the inner region responds more strongly to numerical heating rather than to a genuine improvement in resolution. The present results fit that picture in the sense that the softening length changes the detailed rate of evolution, but not the existence of the decline itself. The tested softening values are $\epsilon = 1$, $5$, $10$, $50$, and $100\ \mathrm{pc}$; shown in Figure~\ref{fig:results_softening} are the 50 Gyr and 100 Gyr snapshots.

This is an important negative result. If softening were the primary cause, the central density evolution should have shown a qualitatively different response when the softening length was varied. Instead, the trend persists across the tested range. The smallest tested softening shows an increase in density as one approaches the center, but that should not be interpreted as a real improvement of the physical problem. The more likely explanation is that the reduced softening has allowed stronger numerical heating and a slightly more concentrated inner profile, which is exactly the kind of bias that an under-softened calculation can introduce \citep{power2003inner,zhang2019optimal}.

\begin{figure}[H]
	\centering
	\begin{minipage}[t]{0.5\textwidth}
	\centering
	\includegraphics[width=\linewidth]{content/ch5/img/grav_soft_study_50.png}
	\end{minipage}%
	\begin{minipage}[t]{0.5\textwidth}
	\centering
	\includegraphics[width=\linewidth]{content/ch5/img/grav_soft_study_100.png}
	\end{minipage}
	\caption{\textbf{Softening length study.} Comparison of the set of tested softening lengths ($\epsilon = 1$, $5$, $10$, $50$, and $100\,\mathrm{pc}$) at 50 Gyr (left) and 100 Gyr (right).}
	\label{fig:results_softening}
\end{figure}

\newpage

\section{Self-gravity settings}
\label{sec:results_solver}

The solver parameter study addresses whether the observed decline could be the cumulative effect of force approximation or integration errors. This is a natural concern in gravity solvers where the acceptance criterion and multipole tolerances determine how accurately long range interactions are represented. In principle, a slightly looser or tighter force approximation can shift the evolution of a halo even when the initial conditions are identical.

The present study varies four key control parameters of the self-gravity solver: timestep factor $\eta$, Multipole Acceptance Criterion (MAC), geometric opening angle $\theta_{\mathrm{cr}}$ and FMM tolerance $\epsilon_{\mathrm{fmm}}$.

The parameter range explored here is broad enough to reveal systematic sensitivity if it exists. The timestep factor was varied by roughly an order of magnitude, the MAC was changed between geometric and adaptive formulations, $\theta_{\mathrm{cr}}$ was tightened in the geometric case, and $\epsilon_{\mathrm{fmm}}$ was varied over two to three orders of magnitude. In a purely numerical explanation, one would expect at least one of these settings to have a decisive effect on the inner profile. Results are represented in Figure~\ref{fig:results_solver}.

\begin{table}[H]
\centering
\caption{Summary of self-gravity solver parameter values in the study. Each row lists the parameter, its baseline value (used in the reference run), and the test values explored to assess sensitivity.}
\label{tab:solver_parameters}
\begin{tabular}{lcccc}
\hline
\textbf{Parameter} & \textbf{Baseline} & \multicolumn{3}{c}{\textbf{Test values}} \\
\hline
$\eta$ (timestep) & 0.002 & 0.0002 & 0.02 & — \\
MAC & geometric & adaptive & — & — \\
$\theta_{\mathrm{cr}}$ (geometric only) & 0.7 & 0.6 & 0.5 & — \\
$\epsilon_{\mathrm{fmm}}$ (FMM tolerance) & 0.001 & 0.0001 & 0.01 & 0.1 \\
\hline
\end{tabular}
\end{table}

\subsection{Timestep study}

The timestep factor $\eta$ determines the frequency of gravity and kinematics updates. Increasing $\eta$ allows larger timesteps and reduces the number of force evaluations per unit time; decreasing $\eta$ enforces finer temporal sampling and can improve accuracy. The baseline value of $\eta = 0.002$ is typical for high-resolution dark matter studies, but it is instructive to test both more conservative ($\eta = 0.0002$, roughly $10\times$ finer) and more permissive ($\eta = 0.02$, roughly $10\times$ coarser) variants.

If timestep error were the main driver of central decline, the coarser run should exhibit notably faster or more severe depletion, while the finer run might show stabilization. That is not observed.

\subsection{Multipole acceptance criterion study}

The MAC choice determines when the force from a distant cell can be approximated by its multipole moments rather than evaluated exactly from individual particles. The \textit{geometric} MAC uses the cell size and distance; the \textit{adaptive} MAC adjusts the criterion based on the local particle distribution. Geometric MAC is faster but less accurate for clumpy structures.

If MAC were the primary sensitivity, changing between geometric and adaptive should produce a qualitatively different central decline rate. The runs tested here show no such distinction.

\subsection{Geometric opening angle study}

For the geometric MAC, a smaller opening angle $\theta_{\mathrm{cr}}$ means stricter multipole approximation. More cell pairs are forced into exact evaluation rather than multipole summation. The baseline value $\theta_{\mathrm{cr}} = 0.7$ is a standard choice; the smaller test values represent a more stringent criterion and therefore higher force accuracy.

Lowering $\theta_{\mathrm{cr}}$ should increase computational cost but also increase force precision. If force approximation error were limiting the simulation, tightening $\theta_{\mathrm{cr}}$ should reduce or delay the central decline. The observed effect is marginal.

\subsection{FMM tolerance study}

The Fast Multipole Method tolerance $\epsilon_{\mathrm{fmm}}$ is a global precision parameter for the multipole expansion. The baseline value is $\epsilon_{\mathrm{fmm}} = 0.001$, and the test suite includes both looser values ($0.01$ and $0.1$, representing $10\times$ and $100\times$ relaxation) and a tighter value ($0.0001$, representing $10\times$ tightening). This range is designed to reveal whether force approximation is a controlling factor.

Some runs with modified $\epsilon_{\mathrm{fmm}}$ exhibit slightly improved early time behaviour, particularly the tighter tolerance, but even those runs exhibit long term central decline. The change is not enough to alter the qualitative long term trend.

\begin{figure}[H]
\centering
\begin{minipage}[t]{0.50\textwidth}
\centering
\includegraphics[width=\linewidth]{content/ch5/img/eta_study.png}
\end{minipage}%
\hfill
\begin{minipage}[t]{0.50\textwidth}
\centering
\includegraphics[width=\linewidth]{content/ch5/img/mac_study.png}
\end{minipage}\\[0.5em]
\begin{minipage}[t]{0.50\textwidth}
\centering
\includegraphics[width=\linewidth]{content/ch5/img/theta_cr_study.png}
\end{minipage}%
\hfill
\begin{minipage}[t]{0.50\textwidth}
\centering
\includegraphics[width=\linewidth]{content/ch5/img/epsilon_fmm_study.png}
\end{minipage}
\caption{\textbf{Self-gravity parameter study.} Panels show the central density evolution for the tested solver settings: timestep factor $\eta$ (top left); MAC choice (top right); geometric opening angle $\theta_{\mathrm{cr}}$ (bottom left); FMM tolerance $\epsilon_{\mathrm{fmm}}$ (bottom right). In each panel blue curves correspond to 10 Gyr and red curves to 100 Gyr.}
\label{fig:results_solver}
\end{figure}

The central density evolution remains broadly similar across all tested settings. This is consistent with the general lesson from convergence studies, changing a gravity control parameter may reduce error bars or shift the onset of bias, but it does not necessarily eliminate the physical or numerical process that dominates the evolution \citep{power2003inner,zhang2019optimal}.

Therefore, the solver settings are not the principal explanation; their influence is secondary. A solver parameter would need to have produced a qualitatively different central decline (either eliminating it or dramatically accelerating it) to be considered a dominant factor. None of the parameters tested here achieve that threshold.

\section{Outer halo influence on inner potential}
\label{sec:results_shell_study}

The central density decline observed in the baseline simulation could, in principle, arise from dynamical interactions between the inner halo and the outer shells. To test this hypothesis, an analytic shell decomposition study using an NFW model \citep{navarro1996structure} is performed to quantify how much of the inner potential depth comes from material at different radii. This analysis then motivates a hybrid numerical experiment: if outer shells are dynamically important, replacing them with an analytic potential while keeping the inner region as particles should produce a different evolution.

\subsection{Key diagnostics and definitions}

The analysis is built around four central diagnostic quantities, each addressing a different aspect of halo structure:

\begin{description}

\item[\textbf{Enclosed mass and shell mass fraction}]
The enclosed mass $M(<r)$ tracks how the total mass budget grows with radius. The shell mass fraction is the mass in a radial shell divided by the total halo mass, showing the radial distribution of the mass budget.

\item[\textbf{Central potential depth from individual shells}]
For each shell at radius range $[r_i, r_{i+1}]$, its contribution to the gravitational potential at the center ($r = 0$) is calculated. This is given exactly by:

\begin{equation}
\Delta\Phi_{\rm shell}(0) = -4\pi G \rho_s r_s^2 \left[ \frac{1}{1 + r_i/r_s} - \frac{1}{1 + r_{i+1}/r_s} \right].
\end{equation}

Shells well outside the center still make sizeable contributions to the potential depth at $r = 0$ (and therefore to the escape-speed scale $v_{\rm esc} \propto \sqrt{-2\Phi}$). The fractional contribution of each shell quantifies this effect.

\item[\textbf{Outer potential fraction}]
For a given probe radius $r_{\mathrm{probe}}$ and cutoff radius $r_{\mathrm{cut}}$, the outer potential fraction is defined as:

\begin{equation}
f_{\mathrm{outer}}(r_{\mathrm{probe}}, r_{\mathrm{cut}}) = \frac{\left|\Phi_{>r_{\mathrm{cut}}}(r_{\mathrm{probe}})\right|}{\left|\Phi_{\mathrm{total}}(r_{\mathrm{probe}})\right|},
\end{equation}

where $\Phi_{>r_{\mathrm{cut}}}$ is the potential contribution from material outside the cutoff and $\Phi_{\mathrm{total}}$ is the total potential at that radius. This measures how strongly the outer halo influences the local potential at the probe radius.

\end{description}

\subsection{Shell diagnostics and decomposition}

%The halo is partitioned into concentric spherical shells and each shell's mass contribution and its influence on the central potential are computed. For shell $i$ with mass $\Delta M_i$ and representative radius $\bar r_i$, the shell contributes approximately
%\begin{equation}
%\Phi_i(0) \approx -G\,\frac{\Delta M_i}{\bar r_i}
%\end{equation}
%to the potential at the origin. By contrast, the radial force at a radius $r$ depends only on the enclosed mass $M(<r)$ by the shell theorem, so outer shells do not change the local force gradient but they do deepen the potential well and therefore the escape speed scale. The shell diagnostics therefore track both $M(<r)$ and the cumulative potential contribution which is useful to quantify non-local coupling between outer mass and inner dynamics.

Since the analysis uses a spherically symmetric Navarro--Frenk--White (NFW) halo model \citep{navarro1996structure}, which is a standard benchmark for dark matter halos:

\begin{equation}
\rho(r) = \frac{\rho_s}{(r/r_s)(1 + r/r_s)^2},
\end{equation}

where $\rho_s$ is the characteristic density and $r_s$ is the scale radius. The enclosed mass within radius $r$ is:

\begin{equation}
M(<r) = 4\pi \rho_s r_s^3 \left[ \ln(1+x) - \frac{x}{1+x} \right], \quad x = \frac{r}{r_s}.
\end{equation}

The halo is truncated at a virial radius $r_{\mathrm{vir}}$. The gravitational potential including the truncation is:

\begin{equation}
\Phi(r) = -\frac{G M(<r)}{r} - 4\pi G \rho_s r_s^2 \left[ \frac{1}{1 + r/r_s} - \frac{1}{1 + r_{\mathrm{vir}}/r_s} \right],
\end{equation}

Rather than relying on a numerical computation of the integrals, the shell analysis uses exact analytic formulas for a truncated NFW profile. The halo is decomposed into logarithmically spaced spherical shells:

\begin{equation}
r_i = r_{\mathrm{min}} \left(\frac{r_{\mathrm{vir}}}{r_{\mathrm{min}}}\right)^{i / N_{\rm shells}}, \quad i = 0, 1, \ldots, N_{\rm shells},
\end{equation}

where typically $N_{\rm shells} = 80$ and $r_{\mathrm{min}} \approx 0.02$ kpc.

For each shell $[r_i, r_{i+1}]$, exact closed form expressions compute: (i) shell mass $M_{\rm shell} = M(<r_{i+1}) - M(<r_i)$, (ii) shell contribution to potential at any radius, and (iii) cumulative potential contributions for integration over shells. For an NFW profile with parameters $\rho_s = 10^7 \, M_{\odot}/\mathrm{kpc}^3$, $r_s = 10 \, \mathrm{kpc}$, $r_{\mathrm{vir}} = 100 \, \mathrm{kpc}$, the results are presented in the following subsections.

\subsection{Mass and potential distribution across the halo}

The first two diagnostics establish the overall radial structure of the halo. Figure \ref{fig:results_shell_mass} shows the enclosed mass profile and the fractional mass distribution per shell. While Figure \ref{fig:results_shell_potential} shows the contribution of each shell to the gravitational potential at the center and the cumulative contribution from all shells outside a given radius. The enclosed mass profile follows the expected NFW form, steep near the center and flattening at larger radii.

\begin{figure}[H]
	\centering
	\includegraphics[width=0.80\textwidth]{content/ch5/img/01_mass_profile_and_shell_fraction.png}
	\caption{\textbf{NFW enclosed mass and shell mass fraction.} Left axis: increasing enclosed mass $M(<r)$ with radius. Right axis: shell mass fraction per logarithmic shell.}
	\label{fig:results_shell_mass}
\end{figure}

\begin{figure}[H]
	\centering
	\includegraphics[width=0.80\textwidth]{content/ch5/img/02_central_potential_shell_contribution.png}
	\caption{\textbf{Shell contributions to the central potential depth.} Blue points represent the contribution of each shell to the total potential depth $|\Phi(0)|$. The cumulative contribution from all shells beyond a given radius is plotted in orange.}
	\label{fig:results_shell_potential}
\end{figure}

%The key finding here is that shells well outside the center, at radii several times the scale radius, still contribute visibly to the potential depth. The fractional contribution decreases with radius but remains non-negligible up to $r \sim 3 r_s$.

\subsection{Outer halo influence as a function of cutoff radius}

The most critical diagnostic for the hybrid model design is the outer potential fraction. The fraction of the total potential at a given probe radius that arises from material beyond a chosen cutoff radius. Figure \ref{fig:results_shell_cutoff} shows this quantity for several probe radii as the cutoff radius is increased.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{content/ch5/img/03_outer_fraction_vs_cutoff.png}
    \caption{\textbf{Outer-potential fraction versus cutoff radius.} For probe radii ranging from the center to $r = 5 r_s$, the fraction of the potential originating from material beyond the cutoff radius $r_{\mathrm{cut}}$ is plotted against the cutoff. As the cutoff moves outward, the outer contribution remains constant initially before dropping sharply as it approaches the outer edge of the system.}
    \label{fig:results_shell_cutoff}
\end{figure}

For the central region ($r_{\mathrm{probe}} < r_s$), setting a cutoff at $r_{\mathrm{cut}} \approx 10$ kpc means the outer halo still contributes roughly 40--60\% of the potential. At $r_{\mathrm{cut}} \approx 30$ kpc, this contribution is still roughly 10--20\%, only dropping below 1\% as the cutoff nears 80--100 kpc. Because the outer halo contributes so significantly to the potential at the inner radii of interest (e.g., $r < 1$ kpc for the central density), the outer region must be represented accurately.

The figure directly addresses the question of how large $r_{\mathrm{cut}}$ must be to capture the essential structure of the potential. Were the outer halo dynamically irrelevant, changes to its structure would produce little or no effect on the inner halo. Even when the cutoff is set to $r_{\mathrm{cut}} = 10\,\mathrm{kpc}$, material lying outside this radius contributes approximately 40--60\% of the potential in the central region. The contribution measured here demonstrates that the outer halo exerts a significant influence on the central potential, making an accurate treatment of the outermost mass distribution essential. This result has two important implications:

\begin{enumerate}
\item \textbf{Motivation for the hybrid model}: A simulation in which the inner region ($r < r_{\mathrm{cut}}$) evolves with particles while the outer region ($r > r_{\mathrm{cut}}$) is represented by an analytic potential can only reproduce the true inner evolution if the outer potential is represented accurately.

\item \textbf{Tests of shell coupling mechanisms}: If the persistent central density decline arises from global halo coupling, where the outer shells modulate the potential experienced by inner particles, then a hybrid model should produce a noticeably different evolution. Conversely, if the decline persists even in the hybrid setup, the mechanism is either purely inner region dynamics or depends on structures (e.g., substructure, asymmetries) not captured by a smooth analytic outer potential.
\end{enumerate}

This is addressed by the hybrid model simulations discussed in the next section.

\section{Hybrid model}
\label{sec:results_truncated_nfw}
This section introduces the truncated-NFW hybrid experiments used to isolate the role of long range potential on the inner halo evolution. The implementation (explained in Section~\ref{sec:framework_hybrid}) is first validated with a small suite of diagnostics to ensure the hybrid coupling behaves as intended. After validation, a systematic study of the truncation radius $R_{\mathrm{cut}}$ and its impact on the inner density evolution is presented; the results of that Rcut parameter sweep are discussed in the following subsections.

\subsection{Mass and potential structure}
As an initial validation of the truncation procedure, the halo mass partition is verified to be correct. For all values of the truncation radius, the measured mass enclosed within $R_{\mathrm{cut}}$ agrees with the expected particle mass, while the remaining halo mass is consistently represented by the analytic potential.

A second validation concerns the resulting gravitational potential (Figure~\ref{fig:multifigure_potential_distr}). The potential distributions obtained for the full halo and for a sequence of truncated models with $R_{\mathrm{cut}} = 50, 25, 10, 5, 3, 2,$ and $1$~kpc show the expected behaviour. As the truncation radius is reduced, the contribution from distant particles is progressively removed and replaced by the analytic component. The sequence of panels clearly illustrate how the potential smoothly approaches the analytic form for sufficiently large cutoffs.

\begin{figure}[H]
    \centering
    \begin{minipage}{0.49\textwidth}
        \centering
        \includegraphics[width=\linewidth]{content/ch5/img/baseline_potential.pdf}
    \end{minipage}
    \hfill
    \begin{minipage}{0.49\textwidth}
        \centering
        \includegraphics[width=\linewidth]{content/ch5/img/potential_01.pdf}
    \end{minipage}

    \vspace{0.2cm}

    \begin{minipage}{0.49\textwidth}
        \centering
        \includegraphics[width=\linewidth]{content/ch5/img/potential_02.pdf}
    \end{minipage}
	\hfill
    \begin{minipage}{0.49\textwidth}
        \centering
        \includegraphics[width=\linewidth]{content/ch5/img/potential_03.pdf}
    \end{minipage}

    \vspace{0.2cm}

    \begin{minipage}{0.49\textwidth}
        \centering
        \includegraphics[width=\linewidth]{content/ch5/img/potential_05.pdf}
    \end{minipage}
    \hfill
    \begin{minipage}{0.49\textwidth}
        \centering
        \includegraphics[width=\linewidth]{content/ch5/img/potential_10.pdf}
    \end{minipage}
\end{figure}

\begin{figure}[H]
     \begin{minipage}{0.49\textwidth}
        \centering
        \includegraphics[width=\linewidth]{content/ch5/img/potential_25.pdf}
    \end{minipage}
    \hfill
    \begin{minipage}{0.49\textwidth}
        \centering
        \includegraphics[width=\linewidth]{content/ch5/img/potential_50.pdf}
    \end{minipage}
	\caption{\textbf{Gravitational potential distributions under baseline and truncated interaction ranges.} Left to right, top to bottom: vanilla \textit{SWIFT} (no truncation), and truncated implementations with $R_{\mathrm{cut}} = 1$, $2$, $3$, $5$, $10$, $25$, and $50$ kpc.}
    \label{fig:multifigure_potential_distr}
\end{figure}

\subsection{Orbits crossing the truncation boundary}

Selected test particles that cross the truncation boundary were integrated to check for inconsistencies caused by the hybrid partition. An example orbit with $R_{\rm cut} = 10$ kpc is shown in Figure~\ref{fig:tnfw_validation_orbits} and remains regular and physically plausible, indicating the hybrid coupling does not introduce obvious integration artefacts on the tested timescales.

Particles that move from the inner region, $r < R_{\rm cut}$, to the outer region, $r > R_{\rm cut}$, between two consecutive simulation snapshots are identified as follows. Let the snapshots be indexed by $i=0,\dots,N_{\rm snap}-1$, with snapshot time $t_i$. For each particle $p$ present in a given snapshot, its position is denoted by
\begin{equation}
\mathbf{x}_{p}^{(i)} = \bigl(x_p^{(i)}, y_p^{(i)}, z_p^{(i)}\bigr),
\end{equation}
and the radius relative to the galaxy centre $\mathbf{x}_{\rm c}$ is defined as
\begin{equation}
r_p^{(i)} = \left\lVert \mathbf{x}_{p}^{(i)} - \mathbf{x}_{\rm c} \right\rVert
= \sqrt{(x_p^{(i)}-x_{\rm c})^2 + (y_p^{(i)}-y_{\rm c})^2 + (z_p^{(i)}-z_{\rm c})^2}.
\end{equation}

An indicator function for whether particle $p$ is inside the cut radius at snapshot $i$ is defined as:
\begin{equation}
I_p^{(i)} =
\begin{cases}
1, & r_p^{(i)} < R_{\rm cut}, \\
0, & r_p^{(i)} \ge R_{\rm cut}.
\end{cases}
\end{equation}

A particle is flagged as a crossing particle if it satisfies an outward transition between two consecutive snapshots,
\begin{equation}
I_p^{(i-1)} = 1
\qquad \text{and} \qquad
I_p^{(i)} = 0,
\end{equation}
for at least one snapshot pair $(i-1,i)$. In other words, the first discrete time interval in which the particle changes from the inner to the outer regime is detected.

To reduce contamination from particles that are not close to the transition boundary, a radial preselection window is applied. Specifically, a particle is only considered a valid crosser if, at the previous snapshot, its radius lies in the interval
\begin{equation}
R_{\rm cut} - \alpha \le r_p^{(i-1)} < R_{\rm cut},
\end{equation}
where $\alpha$ is a user-defined thickness parameter. This ensures that only particles that start close to the boundary are included in the crossing sample.

The detection criterion can therefore be written as
\begin{equation}
\mathcal{C}_p^{(i)} =
\begin{cases}
1, & \text{if } r_p^{(i-1)} \in [R_{\rm cut}-\alpha,\, R_{\rm cut}) \text{ and }
r_p^{(i-1)} < R_{\rm cut} \le r_p^{(i)}, \\
0, & \text{otherwise}.
\end{cases}
\end{equation}
When $\mathcal{C}_p^{(i)}=1$, particle $p$ is recorded as a crossing particle and the snapshot pair $(i-1,i)$ is stored together with the corresponding times $(t_{i-1}, t_i)$.

Since the snapshots are discrete in time, the exact crossing time is not known from the data used here. The implementation therefore identifies the crossing within the interval $[t_{i-1}, t_i]$ rather than interpolating to a continuous crossing time. If needed, a linear estimate of the crossing time could be defined as
\begin{equation}
t_{\rm cross} \approx t_{i-1}
+ \frac{R_{\rm cut} - r_p^{(i-1)}}{r_p^{(i)} - r_p^{(i-1)}}\,(t_i - t_{i-1}),
\end{equation}
but this interpolation is not required for the present selection procedure.

Once the set of crossing particle IDs has been determined, a second pass is performed over all snapshots and the full time series for each selected particle is extracted. For each recorded particle $p$ and snapshot $i$, store
\begin{equation}
\bigl(t_i,\; x_p^{(i)},\; y_p^{(i)},\; z_p^{(i)},\; r_p^{(i)},\;
v_{x,p}^{(i)},\; v_{y,p}^{(i)},\; v_{z,p}^{(i)},\; |\mathbf{v}_p^{(i)}|,\; \Phi_p^{(i)}\bigr),
\end{equation}
where
\begin{equation}
|\mathbf{v}_p^{(i)}| = \sqrt{(v_{x,p}^{(i)})^2 + (v_{y,p}^{(i)})^2 + (v_{z,p}^{(i)})^2},
\end{equation}
and $\Phi_p^{(i)}$ is the particle potential when available in the snapshot output.

This produces a compact data set containing only trajectories relevant to the boundary crossing test. The resulting time series are then used for post-processing diagnostics.

\begin{figure}[H]
	\centering
	\includegraphics[width=0.98\textwidth]{content/ch5/img/orbit_01.png}
	\caption{Orbit of example particle crossing the truncation boundary of $R_{\rm cut} = 10$ kpc. Left to right, top to bottom: $x$ vs $y$, $x$ vs $z$, $y$ vs $z$, and $r$ vs $t$ projections.}
	\label{fig:tnfw_validation_orbits}
\end{figure}

\begin{figure}[H]
	\centering
	\includegraphics[width=0.73\textwidth]{content/ch5/img/summary_orbits.png}
	\caption{Summary of multiple selected orbits crossing the truncation boundary. The vertical dashed line indicates the truncation radius $R_{\rm cut}$.}
	\label{fig:summary_orbits}
\end{figure}

The figure above (Figure~\ref{fig:summary_orbits}) summarizes a larger sample of trajectories. In all cases, the orbits remain smooth and dynamically consistent, with no evidence of discontinuities arising from crossings of the truncation boundary. Additional orbit examples are provided in Appendix~\ref{app:tnfw_orbits}.

In summary, the validation diagnostics show that (i) mass is correctly split by the truncation routine, (ii) the inner potential follows the expected NFW shape (Figure~\ref{fig:multifigure_potential_distr}), and (iii) orbits are not affected by the truncation (Figure~\ref{fig:summary_orbits}). These outcomes indicate that the hybrid model implementation behaves as intended for the described experiments.

\subsection{Density profile}
Following the validation tests, a full 100~Gyr simulation was performed for the hybrid truncated model with $R_{\rm cut}=10$~kpc. The resulting density evolution is shown in Figure~\ref{fig:truncated_10}. The overall behaviour closely follows that of the baseline simulation presented in Section~\ref{sec:results_baseline}, featuring the same decrease in the central density over time.

This result is significant because it demonstrates that the observed central density decline persists even when interactions beyond 10~kpc are removed from the particle-based representation. The mechanism driving the evolution therefore does not appear to depend on the full long range structure of the halo potential, indicating that the relevant dynamics are predominantly governed by processes operating on smaller spatial scales.

\begin{figure}[H]
	\centering
	\includegraphics[width=0.8\textwidth]{content/ch5/img/truncated_10.png}
	\caption{Density profile evolution of the hybrid model with a cutoff of $R_{\rm cut} = 10$ kpc.}
	\label{fig:truncated_10}
\end{figure}

This finding motivates further analysis of smaller $R_{\rm cut}$ values, the results of which is presented in Figure~\ref{fig:large_plot}.

\begin{figure}[H]
	\centering
	\includegraphics[width=1\textwidth]{content/ch5/img/large_plot.png}
	\caption{Study of smaller $R_{\rm cut}$ values. From left to right, top to bottom: $R_{\rm cut} = 5$, $3$, $2$, $1$, $0.5$, $0.3$, $0.2$, $0.1$, $0.05$, $0.03$, $0.02$ and $0.01$ kpc.}
	\label{fig:large_plot}
\end{figure}

The figure above summarizes the density evolution obtained for a range of small truncation radii. For $R_{\rm cut} \gtrsim 0.1\,\mathrm{kpc}$, the evolution remains qualitatively similar to that of the reference (vanilla SWIFT) simulation, with a comparable reduction of the central density over time. However, as $R_{\rm cut}$ approaches the gravitational softening scale, the cusp-to-core transition becomes progressively weaker and no longer persists over the full duration of the simulation.

This trend is illustrated more clearly in the density ratio evolution shown below (Figure~\ref{fig:density_ratio}). While the model with $R_{\rm cut}=5\,\mathrm{kpc}$ continues to exhibit sustained core growth throughout the $100\,\mathrm{Gyr}$ integration, decreasing the truncation radius reduces the effect. For sufficiently small $R_{\rm cut}$, the evolution becomes negligible, and the central density profile remains essentially unchanged.

\begin{figure}[H]
	\centering
	\includegraphics[width=1\textwidth]{content/ch5/img/density_ratio_grid.png}
	\caption{\textbf{Density ratio comparison.} Each panel shows the ratio of the density profile for a given $R_{\rm cut}$ value. The top left panel corresponds to $R_{\rm cut} = 5$ kpc, and subsequent panels show progressively smaller truncation radii down to $R_{\rm cut} = 0.02$ kpc.}
	\label{fig:density_ratio}
\end{figure}

Complete density profile plots can be found in the annex Section~\ref{app:tnfw_density_profiles}.

Finally, the results are summarized in Figure~\ref{fig:r_cut_comparison}, which compares the density evolution for multiple $R_{\rm cut}$ values at 50 Gyr and 100 Gyr. The left panel shows that at 50 Gyr, the runs with $R_{\rm cut} \gtrsim 0.1\,\mathrm{kpc}$ behave very similarly to the baseline, while smaller cutoffs show a more limited decline. The right panel confirms that by 100 Gyr, the same hierarchy persists.

These results indicate that the central density decline is driven by a mechanism that does not require the full long range potential structure of the halo, and that truncating interactions beyond 10 kpc does not eliminate the effect. However, as the cutoff radius approaches the softening length, the core evolution becomes increasingly limited, suggesting that the mechanism is sensitive to interactions at scales comparable to the softening length.

\begin{figure}[H]
	\centering
	\begin{minipage}[t]{0.5\textwidth}
	\centering
	\includegraphics[width=\linewidth]{content/ch5/img/final_50.png}
	\end{minipage}%
	\begin{minipage}[t]{0.5\textwidth}
	\centering
	\includegraphics[width=\linewidth]{content/ch5/img/final_100.png}
	\end{minipage}
	\caption{Comparison of the set of tested $R_{\rm cut}$ values (from $R_{\rm cut} = 1$ kpc down to $R_{\rm cut} = 0.02$ kpc) at 50 Gyr (left) and 100 Gyr (right).}
	\label{fig:r_cut_comparison}
\end{figure}

Studies of high-resolution CDM halos show that even $10^{6}$--$10^{8}$ particles have relaxation times short enough to flatten cusps in a few dynamical times. In particular, \cite{diemand2004relaxation} demonstrated that simulated halos suffer central heating that declines only as $\sim N^{-0.25}$ with particle number, rather than $N^{-1}$, implying that large $N$ ($\gg 10^{8}$) or substantial softening are needed to prevent cusp erosion. Similarly, \cite{elzant2006relaxation} finds that $t_{\mathrm{relax}} \propto r^{2}$ and that a $10^{6}$ particle halo is already noise dominated in the inner $\sim 1\%$, requiring $N \gg 10^{8}$ for the inner profile to remain collisionless.

These works predict that truncating outer mass alone cannot halt inner relaxation; removing long range forces beyond 10 kpc still leaves the small-$N$ central cusp relaxationally unstable. In short, the balance of evidence is that numerical discreteness (two-body scattering and shot noise) is the primary culprit for the persistent core formation in the halo simulations.